# 情感对话数据库结构与关联关系说明

项目：`JmAxbl1MMe4`。更新日期：2026-09-21。来源：Zion 官方 CLI 当前编辑结构快照。

**部署状态：已同步正式 Zion 后端，编辑与部署版本均为 `nk04OPmKL8X`（2026-09-21 回读）。** 当前结构同时包含三类身份管理、旧表清理、公开课场次报名费与课程报名支付订单表。账号、账户资料、工作人员、课程、推荐关系、身份变更记录及 AI 权限已按明确授权配置；课程价格由后端返回。当前 Zion 项目仍未开通微信支付，付费场次未完成真实支付时不会创建成功报名。

原误放的“蝶变践行”文档已原样备份到 `docs/archive/误放的蝶变践行数据库结构-20260908.md`，它不是本项目的结构依据。

## 本次流程与约束

场次详情读取后台报名费 → 免费场次直接报名，付费场次需先生成订单并支付 → 支付回调核验成功后创建报名 → 邀请进群 → 核实实际到课 → 申请咨询 → 后续咨询服务流程。当前已完成金额字段、订单表、动态展示与未支付拦截；真实收银台和支付回调履约仍需在 Zion 支付能力开通后接通。

- 同一账号同一场课程仅一份报名；仅有报名不取得预约资格。
- 每笔课程支付使用唯一订单号、请求幂等键和支付平台交易号防止重复记账；订单金额由服务端按课程报名费生成，不能采用前端传入金额。
- `course_registration_order` 不向匿名用户或普通登录用户开放直接读写，后续仅由受信任的下单动作流和支付回调写入。
- 孩子信息是每次预约的 JSON 快照，服务端只接受姓名、年龄、年级、监护人、关系、电话、困扰、期待字段。
- 老师确认的记录不可覆写；后续补充作为反馈回复保留。
- 总结草稿与录音属于指定预约及请求人；文字来源为本次预约的留言、执行反馈、老师回复。
- `advice` 是 `{key,content}` 数组；`advice_key=conversation` 表示一般留言。
- 总结状态：UPLOADING → QUEUED → PROCESSING → AUDIO_PROCESSING（录音）→ READY / FAILED。READY 仅表示草稿可供检查。
- 音频通过 PRIVATE 预签名 PUT 上传；服务端分配并保存 recording_id，不接受客户端指定任意文件 ID。
- `source_range` 保存文字来源条目范围与是否有更早记录；`conversation_ref` 是服务器分配的 AI 会话编号（文本引用，非外键）。

## 后端动作流

| ID | 用途 | 状态 |
| --- | --- | --- |
| `9f60a0be-4628-4268-a769-661264846cf4` | 同步公开课、资格、预约、资料、反馈、总结任务服务 | 已正式同步 |
| `db7162d9-adc6-412a-ae7d-830d095ae022` | 异步校验总结任务、生成文字草稿或提交音频处理 | 已正式同步 |

两个动作流均从官方登录身份节点读取账号，不接受客户端传入角色作为授权依据。代码保存在 `backend/consultation/`。

## 表清单

| 表 | 显示名 | 状态 |
| --- | --- | --- |
| `account` | 帐户 | 既有结构 |
| `fz_province` | 省份 | 既有结构 |
| `fz_city` | 城市 | 既有结构 |
| `fz_district` | 区 | 既有结构 |
| `fz_audit_record` | 审计路径 | 既有结构 |
| `fz_permission_role` | 角色表 | 既有结构 |
| `fz_account_has_permission_role` | 账户角色表 | 既有结构 |
| `fz_conversation` | 会话 | 既有结构 |
| `fz_message` | 消息 | 既有结构 |
| `fz_tool_usage_record` | 工具使用记录 | 既有结构 |
| `fz_message_content` | 消息内容 | 既有结构 |
| `advisor` | 答主 | 既有结构 |
| `consultation_order` | 咨询订单 | 既有结构 |
| `consultation_session` | 咨询会话 | 既有结构 |
| `consultation_message` | 咨询消息 | 既有结构 |
| `service_provider` | 服务人员 | 既有结构 |
| `account_profile` | 账户资料 | 既有结构 |
| `course` | 课程 | 既有结构 |
| `course_lesson` | 课程课时 | 既有结构 |
| `customer_service_binding` | 客户服务绑定 | 既有结构 |
| `public_class` | 免费公开课 | 本次新增，已正式同步 |
| `public_class_enrollment` | 公开课报名 | 本次新增，已正式同步 |
| `course_registration_order` | 课程报名支付订单 | 2026-09-21 新增，已正式同步 |
| `course_referral` | 课程推荐关系 | 2026-09-15 新增，已正式同步 |
| `offline_appointment` | 线下咨询预约 | 本次新增，已正式同步 |
| `offline_consultation_record` | 线下咨询记录 | 本次新增，已正式同步 |
| `consultation_feedback` | 执行反馈 | 本次新增，已正式同步 |
| `consultation_feedback_reply` | 反馈回复 | 本次新增，已正式同步 |
| `consultation_summary_job` | 沟通总结任务 | 本次新增，已正式同步 |
| `identity_change_log` | 身份变更记录 | 2026-09-21 新增，已正式同步 |

2026-09-21 清理了 4 张未被页面、代码、动作流或关系使用的旧表：`wechat_login_record`（0 条）、`advisor_favorite`（0 条）、`recommendation`（4 条早期“推荐答主”占位记录）和 `ud_banbenshenhe_ebcabf`（1 条早期版本占位记录）。当前登录统一以 `account` 为准，课程推荐使用 `course_referral`。

## 关系清单

以下是平台实际关系，不包含 JSON 业务引用。

| 源表 / 关系字段 | 关系 | 目标表 / 外键字段 |
| --- | --- | --- |
| `account.roles` | ONE_TO_MANY | `fz_account_has_permission_role.account_id` |
| `fz_permission_role.users` | ONE_TO_MANY | `fz_account_has_permission_role.role_id` |
| `fz_conversation.messages` | ONE_TO_MANY | `fz_message.conversation_id` |
| `fz_message.tool_calls` | ONE_TO_MANY | `fz_tool_usage_record.message_id` |
| `fz_message.contents` | ONE_TO_MANY | `fz_message_content.message_id` |
| `account.conversations` | ONE_TO_MANY | `fz_conversation.account_id` |
| `account.messages` | ONE_TO_MANY | `fz_message.account_id` |
| `account.service_provider` | ONE_TO_ONE | `service_provider.account_id` |
| `service_provider.consultation_sessions` | ONE_TO_MANY | `consultation_session.service_provider_id` |
| `account_profile.account` | ONE_TO_ONE | `account.account_profile_id` |
| `course.course_lesson` | ONE_TO_MANY | `course_lesson.course_id` |
| `account.customer_service_binding` | ONE_TO_MANY | `customer_service_binding.customer_account_id` |
| `advisor.customer_service_binding` | ONE_TO_MANY | `customer_service_binding.advisor_id` |
| `service_provider.customer_service_binding` | ONE_TO_MANY | `customer_service_binding.service_provider_id` |
| `advisor.service_provider` | ONE_TO_ONE | `service_provider.advisor_id` |
| `customer_service_binding.consultation_session` | ONE_TO_ONE | `consultation_session.customer_service_binding_id` |
| `service_provider.organized_public_classes` | ONE_TO_MANY | `public_class.organizer_id` |
| `service_provider.verified_enrollments` | ONE_TO_MANY | `public_class_enrollment.verified_by_id` |
| `service_provider.offline_appointments` | ONE_TO_MANY | `offline_appointment.provider_id` |
| `service_provider.offline_records` | ONE_TO_MANY | `offline_consultation_record.author_id` |
| `service_provider.feedback_replies` | ONE_TO_MANY | `consultation_feedback_reply.author_id` |
| `account.public_class_enrollments` | ONE_TO_MANY | `public_class_enrollment.customer_id` |
| `account.course_registration_orders` | ONE_TO_MANY | `course_registration_order.customer_id` |
| `account.sent_course_referrals` | ONE_TO_MANY | `course_referral.referrer_id` |
| `account.received_course_referral` | ONE_TO_ONE | `course_referral.referred_account_id` |
| `account.offline_appointments` | ONE_TO_MANY | `offline_appointment.customer_id` |
| `account.consultation_feedbacks` | ONE_TO_MANY | `consultation_feedback.author_id` |
| `account.summary_jobs` | ONE_TO_MANY | `consultation_summary_job.requester_id` |
| `account.initiated_identity_changes` | ONE_TO_MANY | `identity_change_log.operator_account_id` |
| `account.received_identity_changes` | ONE_TO_MANY | `identity_change_log.target_account_id` |
| `public_class.enrollments` | ONE_TO_MANY | `public_class_enrollment.public_class_id` |
| `public_class.registration_orders` | ONE_TO_MANY | `course_registration_order.public_class_id` |
| `course_registration_order.enrollment` | ONE_TO_ONE | `public_class_enrollment.payment_order_id` |
| `public_class_enrollment.appointments` | ONE_TO_MANY | `offline_appointment.enrollment_id` |
| `offline_appointment.consultation_record` | ONE_TO_ONE | `offline_consultation_record.appointment_id` |
| `offline_appointment.feedbacks` | ONE_TO_MANY | `consultation_feedback.appointment_id` |
| `offline_appointment.summary_jobs` | ONE_TO_MANY | `consultation_summary_job.appointment_id` |
| `consultation_feedback.replies` | ONE_TO_MANY | `consultation_feedback_reply.feedback_id` |
| `consultation_session.summary_jobs` | ONE_TO_MANY | `consultation_summary_job.chat_session_id` |

## 字段与约束

### 帐户 · `account`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | 更新时间 | TIMESTAMPTZ | 是 | 否 |
| `username` | 用户名 | TEXT | 否 | 否 |
| `password` | 密码 | TEXT | 否 | 否 |
| `oauth2_user_info_map` | 用户验证 | JSONB | 是 | 否 |
| `profile_image` | 用户头像 | IMAGE | 否 | 否 |
| `fz_deleted` | 是否注销 | BOOLEAN | 是 | 否 |
| `fz_phone_number` | 电话号码 | TEXT | 是 | 否 |
| `fz_email` | 邮箱 | TEXT | 是 | 否 |
| `wechat_nickname` | 微信昵称 | TEXT | 否 | 否 |
| `wechat_avatar_url` | 微信头像URL | TEXT | 否 | 否 |
| `wechat_openid` | 微信OpenID | TEXT | 否 | 否 |
| `wechat_unionid` | 微信UnionID | TEXT | 否 | 否 |
| `user_type` | 用户类型 | TEXT | 否 | 否 |
| `last_login_at` | 最近登录时间 | TIMESTAMPTZ | 否 | 否 |
| `account_profile_id` | 账户资料_id | BIGINT | 否 | 是 |

### 省份 · `fz_province`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `name` | 名称 | TEXT | 是 | 是 |

### 城市 · `fz_city`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `name` | 名称 | TEXT | 是 | 是 |
| `province_id` | 省份id | BIGSERIAL | 是 | 否 |

### 区 · `fz_district`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `name` | 名称 | TEXT | 是 | 否 |
| `city_id` | 城市id | BIGSERIAL | 是 | 否 |

### 审计路径 · `fz_audit_record`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 是 | 否 |
| `account_id` | 帐户id | BIGINT | 是 | 否 |
| `query_string` | 查询语句 | TEXT | 是 | 否 |
| `variables` | 变量 | JSONB | 是 | 否 |

### 角色表 · `fz_permission_role`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGINT | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 是 | 否 |
| `updated_at` | 更新时间 | TIMESTAMPTZ | 否 | 否 |
| `uuid` | 角色uuid | TEXT | 是 | 是 |
| `name` | 角色名 | TEXT | 是 | 是 |

### 账户角色表 · `fz_account_has_permission_role`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGINT | 否 | 否 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 是 | 否 |
| `account_id` | 帐户id | BIGINT | 是 | 否 |
| `role_id` | 角色id | BIGINT | 是 | 否 |

### 会话 · `fz_conversation`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | 更新时间 | TIMESTAMPTZ | 是 | 否 |
| `account_id` | 帐户id | BIGINT | 否 | 否 |
| `input` | 入参 | JSONB | 是 | 否 |
| `status` | 状态 | TEXT | 是 | 否 |
| `error_message` | 错误信息 | JSONB | 否 | 否 |
| `ai_config_id` | AI配置id | TEXT | 否 | 否 |
| `ai_model` | AI模型 | TEXT | 否 | 否 |

### 消息 · `fz_message`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | 更新时间 | TIMESTAMPTZ | 是 | 否 |
| `account_id` | 帐户id | BIGINT | 否 | 否 |
| `role` | 角色 | TEXT | 是 | 否 |
| `token_usage` | token使用量 | DECIMAL | 是 | 否 |
| `conversation_id` | 会话id | BIGINT | 是 | 否 |

### 工具使用记录 · `fz_tool_usage_record`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | 更新时间 | TIMESTAMPTZ | 是 | 否 |
| `name` | 名称 | TEXT | 是 | 否 |
| `call_id` | 工具调用id | TEXT | 是 | 否 |
| `type` | 类型 | TEXT | 是 | 否 |
| `request` | 请求 | JSONB | 是 | 否 |
| `response` | 响应 | JSONB | 否 | 否 |
| `message_id` | 消息id | BIGINT | 是 | 否 |

### 消息内容 · `fz_message_content`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | 创建时间 | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | 更新时间 | TIMESTAMPTZ | 是 | 否 |
| `type` | 类型 | TEXT | 是 | 否 |
| `text` | 文本 | TEXT | 否 | 否 |
| `image` | 图片 | IMAGE | 否 | 否 |
| `json` | JSON | JSONB | 否 | 否 |
| `message_id` | 消息id | BIGINT | 是 | 否 |
| `video` | 视频 | VIDEO | 否 | 否 |
| `file` | 文件 | FILE | 否 | 否 |

### 答主 · `advisor`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `name` | 姓名 | TEXT | 否 | 否 |
| `title` | 头衔 | TEXT | 否 | 否 |
| `avatar_url` | 头像URL | TEXT | 否 | 否 |
| `bio` | 简介 | TEXT | 否 | 否 |
| `specialties` | 擅长方向 | TEXT | 否 | 否 |
| `tags_json` | 标签JSON | JSONB | 否 | 否 |
| `price_per_hour` | 每小时价格 | DECIMAL | 否 | 否 |
| `rating` | 评分 | DECIMAL | 否 | 否 |
| `consult_count` | 咨询次数 | BIGINT | 否 | 否 |
| `status` | 状态 | TEXT | 否 | 否 |
| `avatar_image` | 头像图片 | IMAGE | 否 | 否 |

### 咨询订单 · `consultation_order`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `order_no` | 订单号 | TEXT | 否 | 否 |
| `customer_account_id` | 客户帐户ID | BIGINT | 否 | 否 |
| `advisor_id` | 答主ID | BIGINT | 否 | 否 |
| `amount` | 金额 | DECIMAL | 否 | 否 |
| `duration_minutes` | 时长分钟 | BIGINT | 否 | 否 |
| `status` | 状态 | TEXT | 否 | 否 |
| `payment_provider` | 支付渠道 | TEXT | 否 | 否 |
| `transaction_id` | 支付交易号 | TEXT | 否 | 否 |
| `paid_at` | 支付时间 | TIMESTAMPTZ | 否 | 否 |
| `chat_available_until` | 聊天可用至 | TIMESTAMPTZ | 否 | 否 |
| `remark` | 备注 | TEXT | 否 | 否 |
| `problem_category` | 问题分类 | TEXT | 否 | 否 |
| `issue_summary` | 问题摘要 | TEXT | 否 | 否 |
| `booking_source` | 预约来源 | TEXT | 否 | 否 |

### 咨询会话 · `consultation_session`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `order_id` | 订单ID | BIGINT | 否 | 否 |
| `customer_account_id` | 客户帐户ID | BIGINT | 否 | 否 |
| `advisor_id` | 答主ID | BIGINT | 否 | 否 |
| `manager_account_id` | 经理帐户ID | BIGINT | 否 | 否 |
| `status` | 状态 | TEXT | 否 | 否 |
| `started_at` | 开始时间 | TIMESTAMPTZ | 否 | 否 |
| `ended_at` | 结束时间 | TIMESTAMPTZ | 否 | 否 |
| `expires_at` | 过期时间 | TIMESTAMPTZ | 否 | 否 |
| `last_message_at` | 最后消息时间 | TIMESTAMPTZ | 否 | 否 |
| `topic` | 会话主题 | TEXT | 否 | 否 |
| `session_source` | 会话来源 | TEXT | 否 | 否 |
| `customer_nickname` | 客户昵称 | TEXT | 否 | 否 |
| `service_provider_id` | 服务人员_id | BIGINT | 否 | 否 |
| `customer_avatar_url` | 客户头像URL | TEXT | 否 | 否 |
| `customer_service_binding_id` | 客户服务绑定_id | BIGINT | 否 | 是 |

### 咨询消息 · `consultation_message`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `session_id` | 会话ID | BIGINT | 否 | 否 |
| `sender_account_id` | 发送者帐户ID | BIGINT | 否 | 否 |
| `sender_role` | 发送者角色 | TEXT | 否 | 否 |
| `content` | 内容 | TEXT | 否 | 否 |
| `content_type` | 内容类型 | TEXT | 否 | 否 |
| `sent_at` | 发送时间 | TIMESTAMPTZ | 否 | 否 |
| `read_at` | 已读时间 | TIMESTAMPTZ | 否 | 否 |
| `client_message_id` | 客户端消息ID | TEXT | 否 | 否 |
| `message_source` | 发送来源 | TEXT | 否 | 否 |
| `delivery_status` | 保存状态 | TEXT | 否 | 否 |
| `is_recalled` | 是否已撤回 | BOOLEAN | 是 | 否 |
| `recalled_at` | 撤回时间 | TIMESTAMPTZ | 否 | 否 |
| `recalled_by_account_id` | 撤回经理账户ID | BIGINT | 否 | 否 |
| `recalled_content` | 撤回原文 | TEXT | 否 | 否 |
| `visible_to_customer` | 客户可见 | BOOLEAN | 是 | 否 |
| `replaces_message_id` | 替换原消息ID | BIGINT | 否 | 否 |
| `replaced_by_message_id` | 已被新消息替换ID | BIGINT | 否 | 否 |

### 服务人员 · `service_provider`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `display_name` | 展示名称 | TEXT | 否 | 否 |
| `title` | 职称 | TEXT | 否 | 否 |
| `avatar` | 头像 | IMAGE | 否 | 否 |
| `avatar_url` | 头像URL | TEXT | 否 | 否 |
| `bio` | 简介 | TEXT | 否 | 否 |
| `specialties_json` | 擅长方向JSON | JSONB | 否 | 否 |
| `service_status` | 服务状态 | TEXT | 否 | 否 |
| `verified` | 是否已认证 | BOOLEAN | 否 | 否 |
| `can_reply` | 允许回复 | BOOLEAN | 否 | 否 |
| `can_accept_order` | 允许接单 | BOOLEAN | 否 | 否 |
| `online_status` | 在线状态 | TEXT | 否 | 否 |
| `rating` | 服务评分 | DECIMAL | 否 | 否 |
| `price_per_hour` | 每小时价格 | DECIMAL | 否 | 否 |
| `service_minutes` | 单次服务分钟 | BIGINT | 否 | 否 |
| `today_waiting_count` | 今日待接单数 | BIGINT | 否 | 否 |
| `active_session_count` | 当前服务中数 | BIGINT | 否 | 否 |
| `today_income` | 今日收入 | DECIMAL | 否 | 否 |
| `total_income` | 累计收入 | DECIMAL | 否 | 否 |
| `last_online_at` | 最近上线时间 | TIMESTAMPTZ | 否 | 否 |
| `approved_at` | 审核通过时间 | TIMESTAMPTZ | 否 | 否 |
| `account_id` | 绑定帐户_id | BIGINT | 否 | 是 |
| `advisor_id` | 答主_id | BIGINT | 否 | 是 |
| `service_kind` | 业务身份 | TEXT | 是 | 否 |

已登录用户只划分为三种业务身份：`管理`、`代理`、`用户`。启用中的 `service_provider.service_kind = STAFF` 且 `can_reply = true`、`can_accept_order = true` 表示管理，`AGENT` 表示代理；没有启用中的工作人员关联记录时为用户。`account.user_type` 仅用于兼容旧界面显示，授权必须以登录账号关联的 `service_provider`、`service_status` 和能力字段为准。身份变更只能由当前管理账号通过后端动作流完成，不能直接从客户端改表。

### 账户资料 · `account_profile`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `user_name` | 用户名 | TEXT | 否 | 否 |
| `avatar_url` | 头像URL | TEXT | 否 | 否 |
| `city` | 城市 | TEXT | 否 | 否 |
| `address` | 详细地址 | TEXT | 否 | 否 |
| `gender` | 性别 | TEXT | 否 | 否 |
| `birthday` | 生日 | DATE | 否 | 否 |
| `phone` | 手机号 | TEXT | 否 | 否 |
| `wechat_avatar_url` | 微信头像URL | TEXT | 否 | 否 |
| `location_info` | 定位信息 | JSONB | 否 | 否 |
| `avatar_image` | 头像图片 | IMAGE | 否 | 否 |

### 课程 · `course`

课程展示与课时目录；二阶共修营的介绍、参与条件、收费说明及学习规则由后台 `description` 维护。`badge` 为展示角标，不构成付费校验或购课权益。

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `title` | 课程标题 | TEXT | 是 | 否 |
| `subtitle` | 副标题 | TEXT | 否 | 否 |
| `description` | 课程详细介绍 | TEXT | 否 | 否 |
| `cover_url` | 封面URL | TEXT | 否 | 否 |
| `cover_image` | 封面图片 | IMAGE | 否 | 否 |
| `duration_text` | 时长展示 | TEXT | 否 | 否 |
| `badge` | 角标 | TEXT | 否 | 否 |
| `sort_order` | 排序 | BIGINT | 是 | 否 |
| `enabled` | 是否上架 | BOOLEAN | 是 | 否 |

### 课程课时 · `course_lesson`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `title` | 课时标题 | TEXT | 是 | 否 |
| `duration_text` | 时长 | TEXT | 否 | 否 |
| `sort_order` | 排序 | BIGINT | 是 | 否 |
| `video_url` | 视频URL | TEXT | 否 | 否 |
| `video` | 视频 | VIDEO | 否 | 否 |
| `course_id` | 所属课程_id | BIGINT | 否 | 否 |

### 客户服务绑定 · `customer_service_binding`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `binding_status` | 绑定状态 | TEXT | 是 | 否 |
| `bound_at` | 绑定时间 | TIMESTAMPTZ | 是 | 否 |
| `transfer_note` | 移交备注 | TEXT | 否 | 否 |
| `last_transferred_at` | 最近移交时间 | TIMESTAMPTZ | 否 | 否 |
| `customer_account_id` | 客户账户_id | BIGINT | 否 | 否 |
| `advisor_id` | 绑定答主_id | BIGINT | 否 | 否 |
| `service_provider_id` | 绑定服务人员_id | BIGINT | 否 | 否 |

### 免费公开课 · `public_class`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `title` | 课程名称 | TEXT | 否 | 否 |
| `description` | 课程说明 | TEXT | 否 | 否 |
| `starts_at` | 开课时间 | TIMESTAMPTZ | 否 | 否 |
| `status` | 发布状态 | TEXT | 是 | 否 |
| `group_guide` | 进群指引 | TEXT | 否 | 否 |
| `signup_url` | 报名链接 | TEXT | 否 | 否 |
| `group_qr` | 进群二维码 | IMAGE | 否 | 否 |
| `organizer_id` | organizer_id | BIGINT | 否 | 否 |
| `cover` | 课程封面 | IMAGE | 否 | 否 |
| `city` | 开课城市 | TEXT | 否 | 否 |
| `contact_phone` | 课程联系号码 | TEXT | 否 | 否 |
| `notice` | 参课须知 | TEXT | 否 | 否 |
| `capacity` | 报名人数上限 | BIGINT | 是 | 否 |
| `reserved_count` | 有效报名人数 | BIGINT | 是 | 否 |
| `revision` | 并发版本 | BIGINT | 是 | 否 |
| `registration_fee` | 报名费用 | DECIMAL | 否 | 否 |
| `registration_closes_at` | 报名截止时间 | TIMESTAMPTZ | 否 | 否 |
| `checkin_closes_at` | 签到截止时间 | TIMESTAMPTZ | 否 | 否 |
| `share_code` | 报名小程序码 | IMAGE | 否 | 否 |

已核实的唯一约束：

- `public_class_id_key`：`id`


### 公开课报名 · `public_class_enrollment`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `registrant_name` | 报名姓名 | TEXT | 否 | 否 |
| `phone` | 联系电话 | TEXT | 否 | 否 |
| `status` | 报名状态 | TEXT | 是 | 否 |
| `attendance_status` | 到课状态 | TEXT | 是 | 否 |
| `group_status` | 进群状态 | TEXT | 是 | 否 |
| `verified_at` | 核实时间 | TIMESTAMPTZ | 否 | 否 |
| `verified_by_id` | verified_by_id | BIGINT | 否 | 否 |
| `customer_id` | customer_id | BIGINT | 否 | 否 |
| `public_class_id` | public_class_id | BIGINT | 否 | 否 |
| `entry_code` | 个人入场码 | TEXT | 否 | 否 |
| `checkin_method` | 核实方式 | TEXT | 否 | 否 |
| `canceled_at` | 取消报名时间 | TIMESTAMPTZ | 否 | 否 |
| `payment_order_id` | 支付订单_id | BIGINT | 否 | 是 |

已核实的唯一约束：

- `public_class_enrollment_id_key`：`id`
- `public_class_enrollment_customer_class_key`：`customer_id`、`public_class_id`
- `public_class_enrollment_entry_code_key`：`entry_code`
- `public_class_enrollment_payment_order_id_key`：`payment_order_id`


### 课程报名支付订单 · `course_registration_order`

该表保存每一笔课程报名支付订单。真实支付接通后，先创建 `PENDING` 订单；支付回调同时校验订单号、实际支付金额、支付状态与幂等状态，只有校验成功才能将订单改为 `PAID` 并创建、关联公开课报名记录。取消、超时或失败订单保留在本表中，便于在 Zion 后台完整追溯。

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `order_no` | 订单号 | TEXT | 是 | 是 |
| `amount` | 应付金额 | DECIMAL | 是 | 否 |
| `amount_paid` | 实付金额 | DECIMAL | 否 | 否 |
| `status` | 订单状态 | TEXT | 是 | 否 |
| `registrant_name` | 报名人姓名 | TEXT | 否 | 否 |
| `phone` | 手机号 | TEXT | 否 | 否 |
| `course_title_snapshot` | 课程标题快照 | TEXT | 否 | 否 |
| `payment_channel` | 支付渠道 | TEXT | 否 | 否 |
| `transaction_id` | 支付平台交易号 | TEXT | 否 | 是 |
| `paid_at` | 支付时间 | TIMESTAMPTZ | 否 | 否 |
| `closed_at` | 关闭时间 | TIMESTAMPTZ | 否 | 否 |
| `expires_at` | 过期时间 | TIMESTAMPTZ | 否 | 否 |
| `request_key` | 请求幂等键 | TEXT | 否 | 是 |
| `failure_message` | 失败原因 | TEXT | 否 | 否 |
| `source` | 订单来源 | TEXT | 否 | 否 |
| `customer_id` | 付款用户_id | BIGINT | 否 | 否 |
| `public_class_id` | 报名课程_id | BIGINT | 否 | 否 |

已核实的唯一约束和显式关联：

- `course_registration_order_id_key`：`id`。
- `course_registration_order_order_no_key`：`order_no`；保证业务订单号不重复。
- `course_registration_order_request_key_key`：`request_key`；保证同一次下单请求不会重复创建订单。
- `course_registration_order_transaction_id_key`：`transaction_id`；保证同一笔支付平台交易不会被重复记账。
- `fk_customer_id_account_id`：`customer_id` 显式关联 `account.id`。
- `fk_public_class_id_public_class_id`：`public_class_id` 显式关联 `public_class.id`。
- `public_class_enrollment_payment_order_id_key`：订单与最终报名记录为一对一关系。
- 匿名用户和普通登录用户对本表的 `select`、`insert`、`update`、`delete`、`count`、`aggregate` 均已关闭。


### 课程推荐关系 · `course_referral`

该表记录微信网页课程分享形成的直属推荐关系。推荐人和被推荐用户都使用 Zion 的正式帐户关联，不以昵称、手机号或前端缓存作为身份依据。

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `locked_at` | 锁定时间 | TIMESTAMPTZ | 否 | 否 |
| `source` | 推荐来源 | TEXT | 否 | 否 |
| `referrer_id` | 推荐人_id | BIGINT | 否 | 否 |
| `referred_account_id` | 被推荐用户_id | BIGINT | 否 | 是 |

已核实的约束和显式关联：

- `course_referral_id_key`：`id`。
- `course_referral_referred_account_id_key`：`referred_account_id`；保证每位用户只能锁定一次推荐关系。
- `fk_referrer_id_account_id`：`referrer_id` 显式关联 `account.id`。
- `fk_referred_account_id_account_id`：`referred_account_id` 显式关联 `account.id`。


### 线下咨询预约 · `offline_appointment`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `request_key` | 申请幂等键 | TEXT | 否 | 否 |
| `requested_time` | 期望时间 | TEXT | 否 | 否 |
| `confirmed_at` | 确认咨询时间 | TIMESTAMPTZ | 否 | 否 |
| `status` | 预约状态 | TEXT | 是 | 否 |
| `contact_name` | 家长姓名 | TEXT | 否 | 否 |
| `phone` | 联系电话 | TEXT | 否 | 否 |
| `concerns` | 本次困扰 | TEXT | 否 | 否 |
| `child_info` | 孩子基础信息 | JSONB | 否 | 否 |
| `staff_note` | 预约说明 | TEXT | 否 | 否 |
| `completed_at` | 完成时间 | TIMESTAMPTZ | 否 | 否 |
| `provider_id` | provider_id | BIGINT | 否 | 否 |
| `customer_id` | customer_id | BIGINT | 否 | 否 |
| `enrollment_id` | enrollment_id | BIGINT | 否 | 否 |

已核实的唯一约束：

- `offline_appointment_id_key`：`id`
- `offline_appointment_request_key`：`request_key`


### 线下咨询记录 · `offline_consultation_record`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `summary` | 沟通内容 | TEXT | 否 | 否 |
| `advice` | 执行建议 | JSONB | 是 | 否 |
| `status` | 记录状态 | TEXT | 是 | 否 |
| `confirmed_at` | 记录确认时间 | TIMESTAMPTZ | 否 | 否 |
| `author_id` | author_id | BIGINT | 否 | 否 |
| `appointment_id` | appointment_id | BIGINT | 否 | 是 |

### 执行反馈 · `consultation_feedback`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `request_key` | 反馈幂等键 | TEXT | 否 | 否 |
| `advice_key` | 执行建议标识 | TEXT | 否 | 否 |
| `content` | 执行情况 | TEXT | 否 | 否 |
| `status` | 回复状态 | TEXT | 是 | 否 |
| `author_id` | author_id | BIGINT | 否 | 否 |
| `appointment_id` | appointment_id | BIGINT | 否 | 否 |

已核实的唯一约束：

- `consultation_feedback_id_key`：`id`
- `consultation_feedback_request_key`：`request_key`


### 反馈回复 · `consultation_feedback_reply`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `request_key` | 回复幂等键 | TEXT | 否 | 否 |
| `content` | 回复内容 | TEXT | 否 | 否 |
| `author_id` | author_id | BIGINT | 否 | 否 |
| `feedback_id` | feedback_id | BIGINT | 否 | 否 |

已核实的唯一约束：

- `consultation_feedback_reply_id_key`：`id`
- `consultation_feedback_reply_request_key`：`request_key`


### 沟通总结任务 · `consultation_summary_job`

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `source` | 总结来源 | TEXT | 否 | 否 |
| `status` | 任务状态 | TEXT | 是 | 否 |
| `transcript` | 转写文字 | TEXT | 否 | 否 |
| `draft` | 总结草稿 | TEXT | 否 | 否 |
| `error_message` | 失败说明 | TEXT | 否 | 否 |
| `recording` | 面谈录音 | FILE | 否 | 否 |
| `consented_at` | 录音同意时间 | TIMESTAMPTZ | 否 | 否 |
| `source_range` | 消息来源范围 | JSONB | 否 | 否 |
| `conversation_ref` | 智能体会话标识 | TEXT | 否 | 否 |
| `request_key` | 总结幂等键 | TEXT | 否 | 否 |
| `requester_id` | requester_id | BIGINT | 否 | 否 |
| `appointment_id` | appointment_id | BIGINT | 否 | 否 |
| `chat_session_id` | chat_session_id | BIGINT | 否 | 否 |

已核实的唯一约束：

- `consultation_summary_job_id_key`：`id`
- `consultation_summary_job_request_key`：`request_key`

### 身份变更记录 · `identity_change_log`

该表只保存管理人员通过受保护动作流完成的身份变更。普通登录用户与游客对该表没有直接查询、写入、修改、删除或统计权限。

| 字段 | 名称 | 类型 | 必填 | 唯一 |
| --- | --- | --- | --- | --- |
| `id` | id | BIGSERIAL | 否 | 是 |
| `created_at` | created_at | TIMESTAMPTZ | 否 | 否 |
| `updated_at` | updated_at | TIMESTAMPTZ | 否 | 否 |
| `old_identity` | 原身份 | TEXT | 是 | 否 |
| `new_identity` | 新身份 | TEXT | 是 | 否 |
| `change_note` | 变更备注 | TEXT | 否 | 否 |
| `operator_account_id` | 操作人账户_id | BIGINT | 否 | 否 |
| `target_account_id` | 目标账户_id | BIGINT | 否 | 否 |

显式关联：`operator_account_id` 和 `target_account_id` 都由 Zion 关系生成并指向 `account.id`。业务身份值仅允许 `MANAGER`、`AGENT`、`USER`，由后端动作流校验。


## 权限与验证边界

- 八张新增业务表（含课程推荐关系）对游客和普通登录用户的直接 CRUD/统计入口已在正式后端关闭；通过服务动作流按本人或负责老师访问。
- `identity_change_log` 对 Anonymous User 与 Logged-in User 的全部直接操作均关闭；只有主服务动作流以服务端权限写入。
- 游客和登录用户对服务人员表直接 insert/update/delete 均已在草稿关闭。前端个人资料保存不再修改服务人员身份，展示名称优先读取关联账号。
- 四张 AI 系统表的登录用户直接读取/统计已在草稿关闭；直接 AI 调用权限仍需核实。未写入真实咨询内容。
- 正式 `account` 权限已按当前登录账号过滤，隔离账号验证跨用户读取和修改均被拒绝；微信网页只更新自身的昵称、头像和微信身份字段。
- 本地业务与权限单元测试、微信编译器检查已经执行；真实后端调用与 AI 文字/合成音频处理已通过；真机录音仍待验收。
- 本次没有发布微信小程序。

## 2026-09-09 课程报名补充

字段及约束来源为本轮官方 CLI 结构，现已同步正式后端。没有新增跨表关系；继续使用课程—报名—账户及核实工作人员的显式关联。

- `capacity=0` 表示不限制人数；`reserved_count` 仅记录有效报名，取消时释放名额；`revision` 通过带版本条件的原子更新防止并发超额和丢失更新。报名与取消均在同步事务内执行。
- `registration_closes_at` 到时停止报名；未填写时以开课时间为准。`checkin_closes_at` 为签到截止，不能早于开课。
- `cover`、`group_qr`、`share_code` 分别是课程封面、进群码和报名小程序码；IMAGE 资产按平台实际字段签名读取，不存临时图片 URL。
- `entry_code` 是后端签发的报名识别码，不包含姓名/手机号；它不是访问客户资料的授权凭据。核销仍需工作人员身份与场次归属校验。取消后旧码失效，重新报名更换码。
- `checkin_method` 区分扫码和人工核实；`verified_at`、`verified_by_id` 留存核实时间及工作人员。已到课不允许改回缺席或取消。
- 本轮核对发现先前复合/请求幂等约束未实际创建，已补齐真实约束，不能仅依赖本地测试中的去重模拟。



## 课程扩展验证补充（2026-09-09）

课程扩展字段及六条业务唯一约束以本轮 `target-model-after.json` 为依据。本轮纠正了旧脚本 `constraintType: unique` 未生效的问题，改用官方 `UNIQUE` 和已读取的字段显示名后创建成功。7 个既有动作流代码节点已更新，结构校验无错误，**现已同步正式后端**。

列表模糊查询使用 Zion 已验证的 `_ilike: {left_operand: {column: ...}, right_operand: {literal: ...}}`，区别于 `_eq` 的类型操作数格式。课程分页为 `id` 降序、每页20条；统计仅限定课程负责人所属场次。版本及唯一约束的真实并发行为仍待正式联调确认。

报名海报 `share_code` 可由负责人调用已核实的 `wechatQRcodeWithParams` 生成并缓存，亦支持手工上传本场真实小程序码；不自动生成微信 URL Link。二维码资产不含家长资料，个人 `entry_code` 仅用于授权范围内的报名查询和核销。取消报名会将个人码置空，重新报名换码。

登录续接数据只在小程序当前进程内暂存，不新增结构表或持久化家长报名表；咨询页未保存输入的保留也仅属于页面状态。


### 最新权限与联调结论（2026-09-09 21:50）

两类默认角色不可直连八张新服务表或直接调用 AI，通过主动作流校验身份/归属后访问；账号仅本人可读写，账户资料按当前账号关联编号过滤，游客不可读取或修改。`CHECK_USERNAME` 在后端查询已验证的 `account.username` / `account_profile.user_name`，只向当前登录用户返回可用性。共用总结 Agent 的 `maxRound` 调整为 2，以支持录音初始化和附件提交；文字与合成录音均返回 READY 草稿。


## 2026-09-11 界面历史记录分页修复

已通过官方 CLI 核对原部署 `vDAB57E4rEM`，只修改主动作流的 `classes` 与 `appointments` 两个代码节点，校验无错误后同步为 `B9K5XJZMbDB`。部署回读确认代码一致；表、字段、关系、唯一约束及数据模型权限未变。平台同时纠正了输出绑定的一处乱码中文显示名，绑定路径与类型未变。

以下参数均位于现有主动作流的 JSON `payload` 中，不是新增数据库字段：

| 操作 | 新界面请求 | 返回分页信息 | 每次请求保留的归属条件 |
| --- | --- | --- | --- |
| `MY_OVERVIEW` | `paginate: true`，后续页传 `appointmentCursor` | `appointments`、`nextAppointmentCursor` | `offline_appointment.customer_id` 为当前登录账号 |
| `STAFF_OVERVIEW` | `paginate: true`，后续页传 `appointmentCursor` | `appointments`、`nextAppointmentCursor` | `offline_appointment.provider_id` 为当前工作人员，仍验证接单/回复能力 |
| `GET_APPOINTMENT` | `paginate: true`，`appointmentId`，可传 `feedbackCursor` 或 `summaryCursor` | `feedbacks`、`nextFeedbackCursor`；`summaryJobs`、`nextSummaryCursor` | 先验证预约归属，再限定反馈/总结的 `appointment_id`；客户只看本人请求的总结 |

- 新界面按 `id` 降序读取，每页20条，查询第21条判断是否有下一页。游标是字符串 ID，下一页使用 `_lt` 并与原有归属条件取交集；末页返回 `null`。翻页过程中新增记录需要刷新才能显示。
- 旧客户端不传 `paginate: true` 时仍保留旧条数，避免单独同步后端影响尚未更新的小程序；新界面具有“查看更多”和已加载/总条数提示。
- 报名与课程仍通过已有的“我的报名”“课程管理”“名单”分页页面访问；概览中的报名/课程摘要保留原有限量读取。
- `offline_appointment` → 账户/工作人员，`consultation_feedback` → 预约，`consultation_summary_job` → 预约/请求人均保持原有**显式关联**。游标不形成新关联，不替代权限检查。
- 本地合成数据覆盖246条预约、225条反馈、45条总结，包含重复/遗漏与跨账号拒绝验证；线上仅使用既有合成账号进行空列表分页契约、旧调用兼容和拒绝访问检查，未创建真实业务记录。


## 2026-09-11 二阶共修课程展示

新增 `course.description`（可空 TEXT，课程详细介绍），用于保存正式课程说明。课程与课时的一对多显式关联及唯一约束不变；图片继续使用已有 `cover_image` 资产关联。字段已同步至正式版本 `XAgrmdNbOvm`，匿名读取课程介绍验证通过。付费课程目前仅展示收费说明，未新增支付订单、建档身份或一阶完课资格模型。

课程 `id=7` 为《透过现象·直击本质》二阶线上共修营·03期，`badge=付费`，`sort_order=0`，封面为空；实际开营日期与报名方式尚待公布。经用户明确确认，`advisor` 的 5–10 号头像引用、`course` 的 1–6 号封面引用已清空，资产原文件、用户头像、旧课程内容和课时关联保留。


## 2026-09-15 微信网页课程与推荐关系

新增独立 H5 网页入口，只保留首页、课程、课程详情、报名和用户中心。网页不读取答主或服务人员卡片；《透过现象·直击本质》二阶线上共修营统一标记为付费课程，实际开营时间未确定前不写入可报名场次。

微信内登录使用公众号网页授权，服务端将公众号 `openid` 映射为稳定的 Zion 帐户并保存签名会话。公众号 AppSecret 和会话密钥只存在部署环境变量，不进入浏览器代码。网页报名继续调用主动作流的 `ENROLL` / `CANCEL_ENROLLMENT`，保留课程状态、截止时间、名额、重复报名和归属校验。

主动作流新增三个操作：

| 操作 | 用途 | 授权范围 |
| --- | --- | --- |
| `LOCK_REFERRER` | 首次微信网页登录后写入推荐关系 | 只能为当前登录帐户写入；拒绝推荐自己，唯一约束阻止改绑 |
| `MY_REFERRAL_STATUS` | 显示当前用户已经锁定的推荐人 | 只按当前登录帐户的 `referred_account_id` 查询 |
| `MY_REFERRALS` | 查看自己直属推荐用户及其课程报名状态 | 只按当前登录帐户的 `referrer_id` 查询，不返回手机号或其他私密资料 |

`course_referral` 对 Logged-in User 和 Anonymous User 的直接 select/insert/update/delete/count/aggregate 均关闭，所有读写只经主动作流的固定字段和当前登录身份完成。推荐链接的 `ref` 是服务端签名的帐户引用；它用于确定推荐来源，不是登录凭证，也不能读取帐户资料。

本轮结构和动作流校验无错误，已部署为 `o0DY56wr2l5`。使用项目既有的隔离账号进行正式接口验证：匿名及登录用户直接读取推荐表均被拒绝；首次锁定成功；再次携带其他推荐链接不会改绑；推荐人只能看到自己的直属用户及其课程报名状态。验证产生的推荐记录已在测试结束时清理。


## 2026-09-21 三类身份管理与旧表清理

管理工作台新增“身份管理”，通过主动作流的 `LIST_ACCOUNT_IDENTITIES` 与 `SET_ACCOUNT_IDENTITY` 读取和修改其他账号的业务身份。服务端只接受 `MANAGER`、`AGENT`、`USER`，并再次核验当前登录账号必须是启用中的 `STAFF`，且同时拥有回复和接单能力；管理不能在此处修改自己的身份。

每次实际变更会在同一动作流事务内同步更新 `account.user_type`、`service_provider`，并写入 `identity_change_log`。改成用户时将既有服务人员记录设为 `INACTIVE` 而不删除，保留历史咨询关系。新表对 Anonymous User 与 Logged-in User 的查询、写入、修改、删除、计数和聚合权限全部关闭。

已删除 4 张未被页面、动作流、代码或关系使用的旧表及其中 5 条早期占位数据；仍被预约、反馈、总结等正式流程引用的空表全部保留。结构和动作流校验无错误，已部署为 `q8zA540PEvJ`。线上回读确认新字段与两个账户关系存在、旧表不再出现在查询入口、身份记录初始为空；无用户会话调用身份列表会被服务端拒绝。刘曜恺的小程序账号和网页账号均保持管理身份。


## 2026-09-21 场次报名费

- `public_class.registration_fee` 为可空 `DECIMAL`，单位是元；老版 Zion 已有表新增字段时不能同时设置必填和默认值，因此服务端将历史 `null` 按 `0`元处理。
- 工作人员在课程维护页可直接修改金额，新建课程表单默认带入 `100`元；支持 `0`–`999999.99`元，最多两位小数。
- 当前可报名场次 `public_class.id=6`已设为 `100`元，正式动作流 `GET_CLASS` 回读为 `registration_fee=100`。
- `ENROLL` 在服务端强制检查该字段；费用大于 0 时，客户端伪造 `paid` 或金额也不会创建报名。当前项目套餐的 `PAYMENT=0`且 `wechatPay=false`，所以尚不存在可信支付成功回调。
- 已新增专用 `course_registration_order` 表，记录订单号、应付/实付金额、状态、支付渠道、平台交易号、付款时间、失败原因和课程/用户/报名关系；匿名及普通登录角色不可直接访问。
- 后续开通支付时仍需在 Zion 中把支付模块绑定到该订单表。该绑定不可替换或解除，因此本轮没有在商户号和回调方案未确认时自行绑定。支付履约必须仅在 `paymentFound=true`、`paymentStatus=SUCCESSFUL`、`alreadyProcessed=false` 且实付金额与订单一致时生成成功报名。
