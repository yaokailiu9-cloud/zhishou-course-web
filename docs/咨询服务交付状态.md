# 客户入口与咨询服务交付状态

更新：2026-09-11。目标 Zion：`JmAxbl1MMe4`；参考 `bZ7yl9DZ4YY` 仅查看，未修改。

## 二阶线上共修课程更新（9 月 11 日晚，当前最新）

- 首页专家卡片已替换为后台付费课程推荐，顶部人物头像替换为“知”字品牌标识；搜索入口转为课程搜索。
- 新增真实课程《透过现象·直击本质》二阶线上共修营·03期（`course.id=7`），详情展示优化后的介绍、参与条件、680/1680元收费及学习规则，并明确原有免费权益不变。开营日期与报名方式待公布，尚未启用付费购买。
- `course.description` 字段已部署，当前 Zion 正式版本 `XAgrmdNbOvm`。数据库关系说明已同步。
- 按确认清空 6 位专家头像及 6 门旧课程封面的后台图片绑定，资产原文件、用户头像、旧课程及课时内容保留。首页、课程列表、搜索与详情使用无图占位。
- 112 项测试、24 WXML / 28 WXSS 官方编译通过；原生首页已核对，读取真实后端二阶课程成功。
- 微信官方已成功上传 `1.0.20260911.2`，122 文件纯源码，编译大小634476字节；微信审核与正式上线仍未完成。最新预览和证据见 `outputs/course-home-2026-09-11/`。

## 最新交付状态（9 月 11 日，以下状态优先于历史记录）

- 正式 Zion 当前版本为 `B9K5XJZMbDB`，已同步课程/工作人员预约列表分页；本轮课程发布修复没有新增表字段或改变后端权限。
- UI 审查整改与课程发布提交修复已合并：109 项测试、24 WXML / 28 WXSS 官方编译通过。具体修改见 `outputs/ui-fixes-2026-09-11/修复清单.md` 和 `outputs/wechat-release-2026-09-11/发布说明.md`。
- 微信官方开发者工具已成功上传 `1.0.20260911`，同一源码的官方预览二维码已生成；编译大小 635971 字节。根目录源码 ZIP 已更新为 121 文件，不包含后端凭证或验收业务数据。
- 分享按微信原生按钮流程发送已保存课程，无需生成外部网页链接。课程发布失败会显示具体业务原因，保留表单；已保存后读取失败不会丢失课程 ID。
- 本次用户报告的发布失败尚未复现到明确根因，也未完成真实手机课程发布复测。诊断时公开课表无记录，未找到对应近期错误请求；没有为排查而发布虚构课程。
- 微信公众平台当前停在管理员扫码登录页。已上传开发版本，尚未提交审核或正式上线；扫码后可继续官方审核发布流程。新版预览二维码：`outputs/wechat-release-2026-09-11/preview-qr.png`。
- 按用户要求，运营主体及隐私联系方式暂不新增。

以下为历史验收记录，其中旧版文件数、版本号和发布渠道以本节最新状态为准。

## 当前实现

- 首页免费公开课报名、客户中心入口已恢复。课程列表、详情、报名表、我的报名、个人凭证、课程编辑、报名名单、核销和分享海报已接入正式后端。
- 公开课免费，姓名和手机号报名；工作人员联系进群，群内通知地址。实际到课并由工作人员核实后，才开放咨询申请；内部付费课程按要求暂缓。
- 工作人员确认预约时间后，家长填写孩子基础信息；老师记录沟通内容和执行建议；家长持续反馈，工作人员逐条回复。
- 文字沟通和面谈录音均可生成总结草稿，由老师检查后确认正式记录。录音退出页面时保留异步停止回调，交回音频后继续提交处理；完成后不会更新已卸载页面，也不会移除其他页面的录音监听器。
- 登录后恢复原课程和当次报名表；刷新不会覆盖未保存的孩子信息、老师记录和 AI 草稿。
- 修正工作台文字挤压、输入文字偏下、默认按钮变窄、申请表位置靠后等问题，补上孩子信息固定字段名。新建课程未保存时不再显示无效的已保存课程预览按钮。

## 已取得的验证证据

| 范围 | 结果与证据 |
|---|---|
| 正式 Zion | 部署版本 `vDAB57E4rEM`，七张服务表、两个动作流；账号本人权限和默认角色直接 AI/课程表访问限制生效。见 `.codex-work/course-reference/deployed-final-state.json`。 |
| 正式业务链路 | 合成账号完成发布、报名、到课核实、咨询资格、预约确认、孩子信息、记录、反馈、回复与完成；并发最后一席、取消释放及重新报名换码通过。见 `.codex-work/consultation/end-to-end-core.json` 和 `course-transactions.json`。 |
| 资料和权限 | 首次资料保存、本人读写、跨账号/游客拒绝、用户名查重、8 项直接访问拒绝通过。见 `.codex-work/consultation/profile-permissions.json` 和 `direct-permissions.json`。 |
| 总结 | 正式后端文字和私有合成录音均生成 READY 草稿，录音转写与内容相符。见 `.codex-work/consultation/summary-results.json`。 |
| 微信身份接口 | 官方 `wx.login` 取得真实临时码，Zion 原生登录复用已有账号成功，该身份可读本人账号及客户中心；未改变当前登录缓存。此项不替代手机登录表单及首次开户验收。见 `.codex-work/course-reference/native-qa/native-login-result.json`。 |
| 本地检查 | 73 项测试通过，包含 3 项录音退出/上传生命周期测试；23 WXML / 27 WXSS 官方编译通过。见 `native-qa/final-tests.txt` 和 `.codex-work/course-reference/compile-results.json`。 |
| 原生页面 | 12 种有数据页面或状态使用本机合成数据检查；课程、报名、凭证、名单、客户申请、孩子信息、老师记录、反馈回复均有原生截图。见 `.codex-work/course-reference/native-qa/`。 |
| 原生图片 | 项目绘码函数实际绘制的入场码已从模拟器截图独立解码。课程海报使用已有签名测试图片成功绘制并生成原生临时图片。海报使用首页测试码，不算实际课程跳转验收。 |
| 手机预览 | 官方 117 文件纯源码预览已生成；用户确认前一版二维码能在手机打开首页。当前二维码及源码包的具体版本见 `.codex-work/course-reference/delivery-self-check.json`。 |

## 仍未完成的验收

- 手机登录及首次账户资料填写仍待用户验证。开发者工具已实际验证客户中心 → 登录页 → 真实微信登录 → 返回客户中心；昵称/头像留空复用现有账户，不改账户资料，缓存检查后完整恢复。证据见 `.codex-work/course-reference/native-login-ui/`。报名场次和未提交表单的返回仍以现有本地测试为证据，尚未宣称手机验收完成。
- 真实手机相机核销、课程分享或场次码跳转、海报保存到相册，以及麦克风录音完整过程。上述能力已有实现和对应的后端/本地证据，尚不能据此宣称设备验收通过。
- 开发者工具启动稳定性：本轮连续原生导航可用，但历史上存在基础库配置/插件网络 `ETIMEDOUT`、500 及偶发无响应。IPv4 独立请求通过，IPv6 曾超时；已将工具代理改为直连并关闭本机热重载，尚未证明唯一根因或彻底修复。

原先的 `APPID_ERROR` 自动化问题已恢复，后续导航、运行时读取、原生截图、预览和 `wx.login` 均已成功。验收日志写入项目曾触发重编译、使页面返回首页；现在先写项目外临时目录，检查结束再归档。

## 交付与边界

- 源码包：`empath-consultation-miniapp-code.zip`，117 文件，不含后端凭证、客户资料或合成验收数据。
- 内置浏览器：`http://127.0.0.1:8876/phone-preview.html` 为手机二维码，`native-review.html` 为新版首页、专家、课程、报名和咨询共 20 张原生截图，根页面为不同屏宽的合成排版预览。
- 正式合成业务记录、资料及临时工作人员身份已清理；四个凭证绑定合成账号、三个平台保护的 AI 会话及测试资产保留并记录。原生页面自查没有创建正式业务记录。
- 真实公开课时间、封面和进群资料由工作人员在课程管理上架；没有编造真实开课安排。支持微信分享、课程小程序码和已有 HTTPS 链接复制，尚未实现自动生成微信 URL Link。
- 已同步的是正式 Zion 后端。本次微信操作为预览，未提交微信审核或发布正式小程序。

逐项功能说明见 `docs/功能落实自查清单.md`；数据结构见根目录 `数据库结构与关联关系说明.md`。整体目标仍未标记完成。

## 最新 UI 改版（9 月 10 日）

按用户确认，沿用原首页、专家与课程内容，重做首页/专家/课程及报名咨询页面。详见《UI视觉改版说明》。23 个 WXML、27 个 WXSS 编译通过，73 项测试通过，192 次浏览器边界检查通过，已完成新版原生截图复核。当前发布包包含此前录音离页生命周期修复；微信手机预览状态以下方最新记录为准。

新版 UI3 微信官方预览已生成，编译大小 600079 字节，二维码已替换。源码包仍为 117 个文件，包含录音离页保存修复；仅为手机预览，未发布正式微信版本。

## 登录界面自查补充（9 月 10 日）

实际点击原生“微信登录”按钮，成功从登录页返回客户中心，客户端读取状态为已登录。仅“去登录”导航确认弹窗使用自动确认，微信登录与 Zion 请求未模拟。原登录缓存完整恢复，私有临时备份已删除；未填写新昵称/头像，未创建课程或咨询记录。这项证据属于开发者工具，仍不替代手机与首次开户验收。

正式 Zion 元数据已再次只读核对：目标项目仍为 `JmAxbl1MMe4`（情感对话），当前正式版本 `vDAB57E4rEM` 与已记录的联调版本一致。

## 品牌名称统一（9 月 10 日）

按用户要求，首页、课程、我的、登录、隐私及导航标题中的旧品牌统一改为“知手”。官方模板与样式编译通过，已核对原生页面并更新展示截图与源码包。

## 前后端适配复核（9 月 10 日，品牌更名后）

只读复核正式 Zion：7 张业务表文档列出的 93 个字段全部存在，报名与课程/客户/核实人、预约与记录/反馈/总结、回复与反馈/作者等关键关系存在。前端现有课程和专家查询均成功，各读取 6 条；这两类公开内容的已查询文本字段没有旧品牌名称。公开课动作流返回成功，当前已上架公开课为 0 场。

本轮 UI 与“知手”更名未新增业务数据字段，没有执行数据库迁移；此前正式业务结构与权限改造继续使用。复核证据见 `.codex-work/course-reference/backend-adaptation/`。本次未修改客户、课程、预约或其他正式业务数据。
