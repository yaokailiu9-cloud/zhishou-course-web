# 小程序网页复刻 · 2026-09-18

## 入口与代码

- 仓库： https://github.com/yaokailiu9-cloud/zhishou-course-web
- 新网页入口：`/web/`（根路径也会进入网页）。
- 原简化课程网页：`/web/legacy/`，保留课程推荐关系、专属推荐链接和原有管理入口；新网页“我的”可进入推荐记录。
- 原小程序：`app.*`、`pages/`、`styles/`、`utils/` 和微信开发者工具配置原样保留；此次移植未修改小程序源码。
- 小程序和原网页的完整本地源码快照：`outputs/replica-backup-20260918/original-miniapp-and-web.zip`，包含开始本任务前的未提交修改。快照和私密配置不上传 GitHub。
- 生成器：`scripts/build-web-replica.py`；浏览器适配：`web/replica/runtime.js`；生成的页面、表达式、业务模块和样式：`web/replica/`。

执行 `npm ci`、`npm run build:web`、`npm test`、`npm start`。构建不修改小程序，产物随仓库提交，托管环境直接执行 `npm start` 即可，不要求部署服务器安装 Python。以后修改小程序页面后需重新构建并提交网页产物。

## 复刻范围

原 `app.json` 注册的 24 个页面全部纳入网页：

首页、课程、课程详情、聊天、我的、经理工作台、订单详情、编辑资料、隐私说明、答主详情、搜索、客户服务、公开课、咨询详情、服务工作台、公开课详情、报名表、我的报名、入场凭证、课程管理、编辑课程、报名名单、课程分享、帮助与支持。

原布局、配色、文字、循环列表、条件状态、课程目录和业务处理复用原源码。浏览器补充了四个底部标签、子页面返回、链接深链、原生日期/选项控件、图片选择/预览、剪贴板、电话链接、二维码图片识别、入场二维码画布和视频播放适配。Web 分享生成网页链接。

## 数据与身份

- 沿用 Zion 项目 `JmAxbl1MMe4`，本次只读核对正式 schema `o0DY56wr2l5` 和课程/课时结构，未修改或同步后端结构。
- 新 `/api/h5?action=graphql` 只携带当前 HttpOnly 会话中的用户 JWT 或匿名身份访问固定 Zion 地址；不使用管理 Token，不转发客户端伪造的 Authorization。
- 接口要求 JSON POST 并校验 Origin；会话摘要不返回 JWT；受保护页面沿用原后台权限和动作流校验。
- 网页登录沿用公众号 OAuth：公开 AppID 用于构造授权地址，微信回调的临时 `code` 交给 Zion `loginWithWechat` 换取账号 JWT；公众号 AppSecret 只保存在 Zion。部署端仍需 `SESSION_SECRET`、`PUBLIC_ORIGIN` 和公众号授权域名。
- 网页和原小程序的微信账号自动合并不属于本次移植；既有 H5 登录方案按公众号身份建立账户，不能宣称自动继承小程序所有用户记录。
- GitHub 协作者 `zhouliujunteng` 已核实为 `write`，可读取和提交仓库。仓库已有 Secret `SECRET`；未读取、打印、替换或扩大密钥权限。

## 验证与边界

- 自动测试覆盖全部 24 页加载、源文件哈希、搜索输入/列表、日期控件、工作人员拦截、会话不泄露、身份转发、跨域请求拒绝、旧网页入口及原项目测试。
- 浏览器在 390×844 手机尺寸核对首页、课程搜索、真实课程介绍/课时、公开课详情及报名截止状态、我的/微信登录引导。
- 未以真实账号提交报名、聊天、修改资料、核销或工作人员数据；登录后写入流程仍需公众号环境实测。真实视频课时缺失时显示原“目录准备中”状态。
- 网页支付和录音编码尚未接通：明确提示，支付在创建订单前停止，录音不产生伪造音频或处理任务。定位采用手动填写地区与地址。
- 本次 GitHub 上传不等同于域名发布已成功；若托管平台监听主分支，需以其部署结果和实际域名验收为准。
