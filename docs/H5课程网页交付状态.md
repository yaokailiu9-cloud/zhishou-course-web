# 知守课程 H5 交付状态

更新日期：2026-09-15

## 已完成

- 新增 `/web/` 移动网页，只保留首页、课程、课程详情、报名表单和用户中心。
- 首页主标题改为“透过现象，直击本质，让父母有道、孩子有路。”
- 二阶课程以《透过现象·直击本质》二阶线上共修营·03期展示，统一标记“付费”，保留建档家长 680 元、未建档家长 1680 元的说明。
- 页面不加载答主、服务人员或人物图片；课程封面使用无人物的排版占位，等待正式封面。
- 网页报名继续使用现有 Zion 课程动作流，保留课程状态、名额、截止时间、重复报名、取消和本人归属校验。
- 新增公众号网页授权服务端入口、HttpOnly 签名会话、专属推荐链接、首次推荐锁定、直属推荐列表和报名状态。
- 新增 Zion `course_referral` 表、两条帐户显式关联及被推荐用户唯一约束。
- 推荐表对匿名和普通用户的直接 CRUD/统计全部关闭，仅能通过受控动作流访问。

## 后端部署

- Zion 正式版本：`o0DY56wr2l5`。
- 结构与动作流校验：无错误。
- 正式接口隔离验证：匿名直读拒绝、登录用户直读拒绝、首次锁定成功、再次链接不改绑、直属列表隔离、被推荐用户可读取自己的绑定，全部通过。
- 验证使用既有隔离账号；产生的推荐记录已清理，未创建课程、报名或真实用户数据。

## 网页验证

- 119 项自动测试全部通过。
- 390 × 844 手机视口检查了首页、课程列表、课程详情和“我的推荐”页面。
- HTML、CSS、JavaScript 和 Vercel 配置语法通过。

## 生成正式链接前需要的外部配置

部署端需要配置 `SESSION_SECRET` 和正式 HTTPS 域名 `PUBLIC_ORIGIN`；`WECHAT_OA_APP_ID` 已按天启无书公众号默认设置为 `wx6dafecca8d5fd24e`，仍可由环境变量覆盖。公众号后台需要把该域名加入网页授权域名，回调路径为 `/api/wechat-oauth-callback`。Zion 项目 `JmAxbl1MMe4` 还需要在微信登录设置中保存同一个公众号 AppID 与对应 AppSecret，网页授权 `code` 由 Zion 的 `loginWithWechat` 换取账号 JWT，网页服务器不再保存公众号 AppSecret。

当前电脑未登录 Vercel。项目已通过 `.vercelignore` 限定部署范围，只上传 H5 静态文件、H5 API、H5 服务端模块和部署配置；小程序源码、Zion 本地状态、测试凭据、日志、文档及历史输出均不在部署包内。
