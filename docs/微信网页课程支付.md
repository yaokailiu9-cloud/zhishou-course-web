# 微信网页课程报名支付

商户号 `1666219884`，公众号 AppID `wx6dafecca8d5fd24e`。密钥仅保存于 Zion 的 `WECHAT_PAY_V2_KEY` Secret，禁止写入仓库、网页、环境示例或日志。

后台 `public_class.registration_fee` 单位为元，新订单从后台取价；支付订单表为 `course_registration_order`。当前场次 6 设置为 100 元。已有待支付订单保留原价，页面在金额不一致时再次确认。

网页在微信内调用收银台；正式回调地址 `https://www.apply.tianqiwushu.cn/api/wechat-pay-notify`。只有经过签名、金额、商户与付款人校验的微信通知或查单响应才会履约。前端取消、失败、未知结果均不生成成功报名。付款记录和报名通过 `payment_order_id` 显式关联，事务与唯一约束防止重复记账。

部署必须使用协作仓库 main 的现有镜像链路。发布后验收网页资源版本及通知路由；再由用户本人在微信内完成一笔真实订单验收。商户已开通不代表所有支付产品、公众号绑定、JSAPI 授权目录都已核验；只有真实下单/手机收银台能覆盖这些条件，接口查单凭据校验与回滚测试不能替代实付验收。

边界：只接通课程网页的公众号 JSAPI 支付，不更改旧咨询支付或小程序支付；不支持浏览器外微信的 H5/MWEB 支付。支付后课程状态变化记为 `PAID_REVIEW`，由工作人员安排或退款；自动退款和无人值守对账暂未接入。未能确认关单的异常订单保留待确认，不能声称取消收银台就已关闭订单。

验证：`node --test scripts/tests/course-payment.test.js`；`node scripts/verify-course-payment.js` 为真实数据库回滚联调，不产生实际扣款；`--live` 使用线上节点回读代码，仍强制回滚。`npm test` 的历史课程文案测试依赖本地 `outputs/course-home-2026-09-11/二阶共修营课程文案.md`，干净检出需提供该既有测试资料。
