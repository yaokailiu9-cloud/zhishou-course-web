# 三端真实后台联调进度

## 2026-09-18 本次完成

- 已将 Git marketplace `functorz-tech/zion-nocode-plugin` 注册为 `zion`，安装并启用 `zion-nocode@zion` 2.7.7。官方 Git 提交 `a244131`。CLI 改为官方新版固定版本，继续使用项目 `JmAxbl1MMe4`。
- 旧版2.1.6缺少权限写入工具；新版已通过 `GET_TABLE_PERMISSION` / `UPDATE_ROLE_TABLE_PERMISSION` 读取、修改并回读。此前编辑器自动操作超时不再阻塞这项工作。
- 游客和普通登录用户：公开课报名、课程推荐关系均关闭直接CRUD/统计；服务人员禁止直接添加、修改、删除，保留已有读取权限。
- 更新7个现有业务代码节点，结构验证无错误；已同步正式版本 `Mk2g0m8X09d`，metadata回读确认已部署。原小程序页面源码未改动，共用后台规则已更新。
- 运行时 `limit:0` 验证游客、客户读取孩子姓名/反馈正文均被拒绝，没有读取真实客户记录。

## 实际数据库业务验证

使用三个独立合成账号：管理人员 `1000000000000016`、代理 `1000000000000017`、客户 `1000000000000018`。联调课程ID为5，报名ID为5。账户密码、JWT保留在忽略目录 `.codex-work/role-demo/zion-accounts.json`（0600），不返回浏览器。

`scripts/verify_role_demo.js` 已验证：

1. 三账号分别真实认证，客户无法指定代理。
2. 管理人员指定代理，代理无法访问工作人员资料接口。
3. 管理人员将未归属客户直接绑定给代理，重复绑定幂等，客户不能改绑。
4. 客户报名并填写合成孩子资料；代理能查看自己的客户，但不能读取孩子详细资料。
5. 工作人员保存草稿，不开始倒计时，客户响应不含反馈正文。
6. 工作人员确认完成后，`feedback_available_at - feedback_confirmed_at` 正好3小时；确认后不能覆盖。

本次反馈开放时间为北京时间 **2026-09-18 20:49:42.274**。真实时间到期后的读取尚未验收；本地时间边界测试通过。没有快进或修改真实服务器时间。

## 三端入口

- 管理人员：http://localhost:3001/
- 代理：http://localhost:3002/
- 客户：http://localhost:3003/

原本机SQLite演示进程已停止，新进程通过各自Zion账号JWT调用同一项目动作流，使用独立HttpOnly会话。三个页面均提供“登录后台测试账号”按钮；当前数据库已走完一套示例，可检查已绑定、已提交、等待开放的状态。

联调接口仅允许操作指定合成代理、客户与课程。请求按顺序发送，避免连续调用触发Zion HTTP429；不会自动重试有副作用的写操作。

二维码使用本机localhost地址，仅限这台电脑演示。正式网页的完整资料表单、公开域名二维码入口及真实微信OAuth整体验收仍未完成，不能把联调页当成正式网页已经发布。

## 验证证据

- 本地测试137/137通过。
- 忽略目录 `.codex-work/referral-intake/` 保存权限前后快照、同步记录、正式metadata、真实业务验证结果和测试输出。
- 原SQLite文件仅保留历史演示记录，不迁移成真实客户数据。
